# LAN address of the Arduino R4 running the CiPASS sketch.
# Find this in the Serial Monitor right after the board connects to WiFi.
ARDUINO_WS_URL=ws://192.168.1.42:81

# Port the bridge listens on for browser/ngrok connections.
PORT=3001
