# Set this to your Arduino R4's IP:port once it's running the CiPASS sketch.
# Example: ws://192.168.1.42:81
# When left unset, the app uses mock posture data so you can develop the UI without hardware.
VITE_ARDUINO_WS_URL=
